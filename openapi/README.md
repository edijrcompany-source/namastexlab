# openapi/ — contratos API-first (Etapa 5)

`agent-api.yaml` (inbound — nossa API) · `quote-api.yaml` (as-consumed — o que
esperamos do legado). Fonte única: types gerados, mock Prism, drift test.
Lint: `make contracts-lint`. Semver do contrato: docs Etapa 5 §4.
