# dataset/ — Bronze e Silver (GITIGNORED)

Gerado por `make bronze` (seed 42 do gerador do desafio, via
NAMASTEX_CHALLENGE_DIR) e `make silver` (masking + normalização — US-13).
**Nunca commitado** (Etapa 11 §1). Bronze = cru restrito; Silver = PII mascarada
(única base de evals/few-shot).
