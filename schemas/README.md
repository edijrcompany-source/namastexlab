# schemas/ — contratos de eventos públicos (Etapa 9 §3)

6 JSON Schemas v1 (`schemas/eventos/*.v1.json`) — envelope com event_version,
compatibilidade aditiva, PII-safe (só cep_prefixo). Nascem com a implementação;
validados em CI por fixture.
